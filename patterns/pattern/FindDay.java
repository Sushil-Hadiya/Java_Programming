package pattern;

import java.util.Scanner;

public class FindDay {

	String day[] = { "Sun", "Mon", "Tue", "Wed", "Thur", "Fri", "Sat" };

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int d1 = sc.nextInt();
		int d2 = sc.nextInt();
		int odd = Math.abs(d1 - d2);
		int c = 0;
		if (odd % 4 == 0) {
			
		}
	}

}
